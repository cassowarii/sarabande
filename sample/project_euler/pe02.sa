let next_fibonacci = (=> {
  let fib_a = 0
  let fib_b = 1
  return => {
    let temp = fib_a
    fib_a = fib_b
    fib_b = fib_b + temp
    fib_b
  }
})()

let accumulator = 0

repeat {
  let f = next_fibonacci()
  accumulator = accumulator + f if f %% 2
} while f < 4_000_000

accumulator

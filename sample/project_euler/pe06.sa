def sum list {
  let acc = 0
  list.each => elem {
    acc = acc + elem
  }
  acc
}

def square num {
  num * num
}

def sum_squares list {
  list.map(square) | sum
}

def square_sum list {
  sum list | square
}

list::iota 1, 101 | (square_sum _) - (sum_squares _) | println
